#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
    
    // reverse a string
    string str = "abhinash";
    reverse(str.begin(), str.end());
    cout << str << endl;

    // reverse an strng using + algorithm header file
}
